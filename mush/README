MUSH: minimally useful shell

usage: ./demo [ scriptfile ]

Authors: Robert Hensley (rehensle)
         Dustyn Zierman-Felix (dzierman)

   Specific Limits:
   Command Line Length:       512 Bytes
   Commands in a pipeline:    10
   Arguments to a command:    10

About mush:
-If mush is run with no arguments, it'll read from stdin until EOF
-If mush is run with an argument, it will read its commands from the argument as a file
-mush supports redirection of stdin and stdout from or into files
-mush supports pipes
-mush has a built in cd command that must will throw an error if an argument in absent after cd
-support for SIGINT, and appropriate response to it
